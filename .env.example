# Backend API (no trailing slash). Copy to .env and adjust.
# Local:
VITE_API_URL=https://dhiyogram-backend.onrender.com
# Production (Vercel): set the same key in Project → Settings → Environment Variables
# VITE_API_URL=https://your-service.onrender.com
